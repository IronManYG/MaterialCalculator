# v1.3.0 — Play Store shipping walkthrough

Operational runbook for promoting v1.3.0 from `master` (versionCode 6, versionName 1.3.0) to Production on the Play Store. Use this checklist live during the upload — strike through each item as you complete it. Future v1.4.0+ runbooks belong alongside this one as `docs/releases/v1.4.0.md` etc.

## Prerequisites verified at write-time (2026-05-13)

| Item | Status |
|---|---|
| Code on `master` | ✅ versionCode 6, versionName "1.3.0" — PR #33 merged (commit `9aeed42`) |
| AAB built + signed locally | ✅ `app\build\outputs\bundle\release\app-release.aab` — 5.71 MB |
| Marketing screenshots | ✅ 20 PNGs in `screenshots/` (5 surfaces × 2 themes × 2 locales) |
| Upload keystore | ✅ `C:\Users\hussa.HUSSAIN-YG\.android\keystores\materialcalculator-upload.jks` — wired via `local.properties`, verified by successful `signReleaseBundle` task |
| Play App Signing | ✅ Enabled — Google holds the distribution signing key |

If any of the above changes (rebuild, fresh checkout, etc.), regenerate the AAB:

```powershell
./gradlew :app:bundleRelease
```

The AAB lands at `app\build\outputs\bundle\release\app-release.aab` and is signed with the upload key from `local.properties`.

## Step 1 — Internal testing track upload

Always upload to Internal first. Never push straight to Production — this is the validated rollout sequence from the v1.2.0 ship and is documented in memory `project_signing_and_rollout.md`.

1. Open **Play Console** → select the **Sifr (Material Calculator)** app.
2. Left sidebar → **Testing → Internal testing**.
3. **Create new release** button (top right).
4. Drag-drop `app\build\outputs\bundle\release\app-release.aab` into the **App bundles** section.
5. Play Console validates the AAB (versionCode 6, namespace, signing) — takes 30–60 seconds.
6. **Release name** auto-fills as `6 (1.3.0)` — leave it.
7. **Release notes** — paste the bilingual notes from §Release notes below.
8. **Save** → **Review release** → **Start rollout to Internal testing**.

## Step 2 — Smoke test via Play Store Internal install

1. On the test device (Honor 400 Pro is the primary per memory `reference_honor_400_pro_haptics.md`): open the Play Store app.
2. Search **Sifr** or open the listing URL — should see "Internal testing" tag if the account is enrolled as an internal tester (per Phase 1 setup).
3. Tap **Update** — pulls versionCode 6 over live versionCode 5.
4. 5-minute smoke test:
   - Open Sifr; verify new launcher icon + name.
   - Basic: `12 + 5 =` → `17`.
   - Toggle Scientific (🧪 chip) → `sin(30) =` → `0.5` (degrees mode default).
   - Memory: type `42`, M+, MC, MR → cycle works (M chip lights up after M+, clears after MC, restores after MR).
   - Rotate to landscape — keypad + scientific block side by side; status bar hidden.
   - Settings → Theme: System / Light / Dark — all three apply immediately app-wide.
   - History — open via the History icon, see the recent calculations from the smoke test.
   - Switch device locale to Arabic in system settings → app strings flip to Arabic, keypad stays LTR-locked, back arrow mirrors correctly.

Stop the smoke test once everything looks correct. If any check fails — pause shipping, investigate, rebuild and re-upload.

## Step 3 — Refresh listing screenshots (BEFORE production promotion)

Listing changes propagate within minutes — no review delay for screenshot/text-only updates. Do this before promoting Internal → Production so users on day-one of v1.3.0 see the right shots.

1. Play Console → **Grow → Store presence → Main store listing**.
2. Top of page → switch language to **English (United States)** if not already.
3. Scroll to **Phone screenshots**.
4. Delete the existing v1.2.0-era "Material Calculator" shots.
5. Drag-drop 8 PNGs from `screenshots/` in this order:

| Slot | File |
|---|---|
| 1 | `Marketing — Basic, EN, Light.png` |
| 2 | `Marketing — Scientific, EN, Light.png` |
| 3 | `Marketing — Landscape, EN, Light.png` |
| 4 | `Marketing — History, EN, Light.png` |
| 5 | `Marketing — Settings, EN, Light.png` |
| 6 | `Marketing — Basic, EN, Dark.png` |
| 7 | `Marketing — Scientific, EN, Dark.png` |
| 8 | `Marketing — Landscape, EN, Dark.png` |

Why this order: marquee (Basic Light) first, then v1.3.0 hero feature (Scientific), then adaptive-layout signal (Landscape), then practical-value surfaces (History, Settings), then the Dark-mode variants. Drops `History, Dark` and `Settings, Dark` — near-identical inversions, low marginal value.

6. **Save** at the bottom of the English tab.
7. Language dropdown → **Arabic** locale tab.
8. Repeat with the AR equivalents (`Marketing — Basic, AR, Light.png` etc., same order).
9. **Save**.

## Step 4 — Promote Internal → Production

1. Once Step 2 smoke test passes — Play Console → **Production → Releases → Create new release**.
2. Top right → **Promote from Internal testing** → select the v1.3.0 internal release. AAB is reused; no re-upload.
3. Release notes — Play Console offers to copy from the Internal release. Accept.
4. **Review release** → **Start staged rollout**.
5. **Recommended rollout %: 20% for the first 24h.** Safety margin given v1.3.0 is a big jump from v1.2.0 (rebrand + Phase 2.0 → 2.10 features).
6. Submit for Google review. Usually clears in a few hours, occasionally up to 24h.

## Step 5 — Watch Vitals + ramp to 100%

1. 24h after rollout starts → Play Console → **Quality → Android vitals**.
2. Targets:
   - Crashes: < 0.1% sessions
   - ANRs: < 0.05% sessions
   - Excessive wakeups: dashboard green
3. If quiet — Production → Releases → edit the v1.3.0 release → **Increase rollout** → 50% → wait another 12–24h → 100%.
4. If a crash spike — **halt rollout immediately**, investigate via Play's crash dashboard (per memory `user_project_purpose_sifr.md`, the crash-dashboard learning loop is an explicit goal for this project).

## Release notes

**Play Console enforces a 500-character limit per language.**

### `<en-US>` — 435 chars, ready to paste

```
What's new in Sifr 1.3.0:
• New name + identity: "Sifr" (صفر, Arabic for zero)
• Scientific mode: sin/cos/tan, π, e, log, ln, √, factorial
• Memory keys: MC, M+, M−, MR
• Adaptive landscape — basic + scientific side by side
• Editable cursor — tap anywhere in the expression to edit
• Live result preview while typing
• History of past calculations
• Theme + haptics + sound settings
• Polished Arabic locale support
```

### `<ar>` — draft, review before pasting

```
ما الجديد في صفر 1.3.0:
• اسم وهوية جديدة: "صفر" (Sifr)
• الوضع العلمي: الجيب، جيب التمام، الظل، π، e، اللوغاريتم، الجذر، المضروب
• مفاتيح الذاكرة: MC، M+، M−، MR
• تخطيط أفقي متكيف — الأساسي والعلمي جنباً إلى جنب
• مؤشر قابل للتحرير — انقر في أي مكان لتعديل التعبير
• معاينة النتيجة مباشرة أثناء الكتابة
• سجل الحسابات السابقة
• إعدادات السمة والاهتزاز والصوت
• تحسين دعم اللغة العربية
```

## v1.3.0 user-facing changes (for marketing / blog posts)

Rolled up from Phase 2.0 → 2.10 (PRs #12 through #26 plus the post-release cleanups #29/#30/#31/#32):

- **Sifr rebrand** — new name + identity (Arabic: صفر = zero). Previous name "Material Calculator" was a generic dev-tutorial-era placeholder.
- **Scientific mode** — trigonometric functions (sin, cos, tan + inverses), π, e, log, ln, √, factorial. Toggle via 🧪 chip; persists via DataStore. Always visible in landscape regardless of toggle.
- **Memory keys** — MC, M+, M−, MR. M chip in display lights when memory is non-zero.
- **Adaptive landscape** — `WindowSizeClass`-driven layout. Basic + scientific blocks side by side; mirrored under Arabic RTL.
- **Editable cursor** — tap anywhere in the expression to position the cursor and type/delete at that position (Phase 2.9).
- **Live result preview** — partial results render while you type, before pressing `=`.
- **Auto-shrink display** — long expressions shrink rather than overflow.
- **History** — past calculations persisted via Room. Tap-to-restore. Clear-all with confirmation.
- **Settings** — theme (System/Light/Dark), haptics on/off, error sounds on/off. All persist via DataStore.
- **Improved Arabic locale** — RTL-correct toolbar positioning, absolute-left divider, LTR-locked keypad, Arabic translations for all visible strings.

## Future CI/CD — Phase 2.11+ work

Phase 2 backlog (memory `project_signing_and_rollout.md`) records the chosen release tooling:

- **AndroidPoet/playconsole-cli (`gpc`)** for the CLI side — https://github.com/AndroidPoet/playconsole-cli
- **GitHub Actions** for orchestration:
  - `on: push to staging` → publishes AAB to the Internal track automatically.
  - `on: release` (or manual workflow_dispatch) → promotes Internal → Production with policy-gated rollout %.

Once these are wired, the 4-branch flow becomes mandatory (`development → staging → master`) and the Phase 1 dev-to-master exception (this release's path) retires. Natural target: **v1.4.0**. Schedule as part of Phase 2.11 or 2.12.

## Tags

`v1.3.0` annotated tag exists from 2026-05-12, pointing at the original "release: prepare 1.3.0" commit (pre-PR-#33). The tag is **not** moved to the post-#33 master tip — the deltas between the tag and master tip are all preview-tooling / docs / theme / build-config polish with zero user-facing app behavior change, so the tag still correctly identifies the "approved-for-release source state" semantically. The shipped AAB is built from master tip and is functionally identical to a build from the tag commit.
