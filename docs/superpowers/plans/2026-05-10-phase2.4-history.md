# Phase 2.4 — Implementation plan

**Date:** 2026-05-10
**Spec:** `docs/superpowers/specs/2026-05-10-phase2.4-history.md`
**Branch:** `feature/phase2.4-history` off `development`

---

## Commit sequence

Each task is one atomic commit. Order matters: every commit must leave the
build green at HEAD so we can bisect on regressions.

### 1. Spec + plan

- Add `docs/superpowers/specs/2026-05-10-phase2.4-history.md`
- Add `docs/superpowers/plans/2026-05-10-phase2.4-history.md`
- **Commit:** `Add Phase 2.4 spec + plan (History feature)`

### 2. Catalog: Room + KSP

- `gradle/libs.versions.toml`: add `room`, `ksp` versions; add `androidx-room-*`
  libraries; add `ksp` plugin alias.
- `app/build.gradle.kts`: apply `alias(libs.plugins.ksp)`; add Room
  `implementation` + `ksp` deps; add `ksp { arg("room.schemaLocation", ...) }`;
  register schema directory as a debug asset source-set.
- `./gradlew :app:assembleDebug` — verifies plugin + KSP wire correctly even
  before any `@Database` classes exist.
- **Commit:** `Add Room 2.8.4 + KSP 2.3.7 to catalog and app build`

### 3. Domain: HistoryEntry + HistoryRepository contract

- `core/domain/history/HistoryEntry.kt` — pure-Kotlin data class.
- `core/data/history/HistoryRepository.kt` — interface (`observe`, `add`,
  `delete`, `clear`).
- **Commit:** `Add HistoryEntry domain + HistoryRepository contract`

### 4. Data: Room entity, DAO, AppDatabase, mappers, impl

- `feature/history/data/HistoryEntity.kt`
- `feature/history/data/HistoryDao.kt`
- `feature/history/data/HistoryMappers.kt` — `HistoryEntity.toDomain()` +
  reverse helpers.
- `core/data/database/AppDatabase.kt` — `@Database(entities = [HistoryEntity::class], version = 1, exportSchema = true)`.
- `feature/history/data/HistoryRepositoryImpl.kt` — wraps DAO; `add()` builds
  the entity with `System.currentTimeMillis()`.
- `./gradlew :app:assembleDebug` — KSP runs and generates Room implementations.
- **Commit:** `Add Room data layer (Entity / DAO / AppDatabase / repo impl)`

### 5. Cross-feature bus

- `core/data/calculator/CalculatorInputBus.kt` — `Channel<String>(BUFFERED)` +
  `events: Flow<String>` + `suspend fun emit(...)`.
- **Commit:** `Add CalculatorInputBus (cross-feature one-shot expression bus)`

### 6. DI wiring

- `core/data/di/CoreDataModule.kt`: add `single<AppDatabase>` (Room.builder),
  `single<HistoryDao>` (delegated to DB), `singleOf(::CalculatorInputBus)`.
- `feature/history/di/HistoryModule.kt`: new file —
  `singleOf(::HistoryRepositoryImpl) { bind<HistoryRepository>() }` +
  `viewModelOf(::HistoryViewModel)` (placeholder; VM lands in step 7 — keep
  this commit on its own and order step 7 after).
- *(Hold off on adding to `appModules` until VM exists, otherwise Koin fails to
  resolve.)*
- **Commit:** `Wire AppDatabase + HistoryDao + CalculatorInputBus in coreDataModule`

### 7. UI: full History MVI cycle

- `feature/history/ui/HistoryState.kt` — `entries: List<HistoryEntry>`,
  `isLoading: Boolean = true`, `showClearConfirm: Boolean = false`.
- `feature/history/ui/HistoryAction.kt` — `EntryClicked(entry)`,
  `DeleteEntry(id)`, `ClearAllClicked`, `ConfirmClearAll`,
  `DismissClearConfirm`, `BackClicked`.
- `feature/history/ui/HistoryEvent.kt` — `NavigateBack`.
- `feature/history/ui/HistoryViewModel.kt` — observes
  `HistoryRepository.observe()`; on `EntryClicked`, calls
  `bus.emit(entry.result)` then `_events.send(NavigateBack)`.
- `feature/history/ui/HistoryScreen.kt` — Root + Screen in one file. Top app
  bar with back arrow + "Clear all" `IconButton` (only when list is non-empty).
  `LazyColumn` of `HistoryItem` with `key = { it.id }`. Empty state composable.
  `AlertDialog` for clear-all confirmation.
- `feature/history/ui/components/HistoryItem.kt` — `Row` with expression,
  result, formatted timestamp, and a per-row delete `IconButton`.
- Add `historyModule` to `appModules` in `di/AppModule.kt` now that VM exists.
- **Commit:** `Add History UI (State/Action/Event/VM/Root/Screen + HistoryItem)`

### 8. Calculator -> History seam

- `feature/calculator/domain/CalculatorAction.kt`: add
  `data object HistoryClicked : CalculatorAction` and
  `data class RestoreExpression(val value: String) : CalculatorAction`.
- `feature/calculator/domain/ExpressionWriter.kt`: add `RestoreExpression`
  branch (`expression = action.value`); add `HistoryClicked -> Unit` no-op
  (mirrors `SettingsClicked` pattern).
- `feature/calculator/ui/CalculatorEvent.kt`: add `NavigateToHistory`.
- `feature/calculator/ui/CalculatorViewModel.kt`:
  - Constructor-inject `HistoryRepository` and `CalculatorInputBus`.
  - `init { viewModelScope.launch { bus.events.collect { onAction(RestoreExpression(it)) } } }`
  - `HistoryClicked` → emit `NavigateToHistory`.
  - For non-nav actions, capture `pre = writer.expression` *before*
    `processAction(...)`. After: if action was `Calculate` and
    `writer.expression != "Error"` and `pre.isNotBlank()` and
    `pre != writer.expression`, fire-and-forget `historyRepo.add(pre, writer.expression)`.
- `feature/calculator/di/CalculatorModule.kt`: VM constructor changed → Koin
  resolves the new params automatically because we use `viewModelOf(::...)`.
- **Commit:** `Wire Calculator <-> History seam (action surface + write on =)`

### 9. Navigation 3 + history icon

- `navigation/Routes.kt`: add `@Serializable data object HistoryRoute : NavKey`.
- `navigation/NavRoot.kt`:
  - Pass `onNavigateToHistory = { backStack.add(HistoryRoute) }` to
    `CalculatorRoot`.
  - Add `entry<HistoryRoute> { HistoryRoot(onNavigateBack = { backStack.removeLastOrNull() }) }`.
- `feature/calculator/ui/CalculatorScreen.kt`:
  - Replace the single settings `IconButton` with a `Row` containing history
    + settings icons, both `Modifier.align(Alignment.TopEnd)`.
  - Use `Icons.Default.History` (M3 default-icon set).
  - Both wrapped in `dropUnlessResumed { ... }`.
- `CalculatorRoot` accepts `onNavigateToHistory: () -> Unit` parameter and
  routes the `NavigateToHistory` event.
- **Commit:** `Add HistoryRoute + history icon and Nav3 wiring`

### 10. Strings (en + ar)

- `app/src/main/res/values/strings.xml` — append all `history_*` and
  `calc_open_history` keys.
- `app/src/main/res/values-ar/strings.xml` — same keys, Arabic translations
  per spec §3.8.
- **Commit:** `Add History strings (en + ar)`

### 11. Final verification gate

- `./gradlew clean assemble` — debug, staging, release
- `./gradlew test` — 16 existing tests still pass
- `./gradlew :app:assembleDebugAndroidTest`
- `./gradlew :app:lintDebug` — 0 errors
- `./gradlew :app:bundleRelease`
- Manual smoke per spec §5 (user runs).
- If lint flags any leftover issues, fix in a final small commit:
  **Commit:** `Lint fixes: <whatever was flagged>`

## Verification matrix

| Concern | Verified by |
|---|---|
| Room schema exports | `app/schemas/dev.gaddal.sifr.core.data.database.AppDatabase/1.json` exists after debug build |
| KSP wired | `:app:kspDebugKotlin` task appears in `./gradlew tasks --all` |
| Repo writes via DI | App startup doesn't crash; clicking `=` produces a row visible in History |
| Cross-feature seam works both ways | Calculate writes; tap restores |
| Boundary rule 1 (no feature → feature import) | `grep -RE "import dev.gaddal.sifr.feature.history" app/src/main/java/dev/gaddal/sifr/feature/calculator/` returns nothing |
| Lint MissingTranslation stays green | `:app:lintDebug` has 0 errors |

## Rollback

Single feature branch, single PR. Reverting the merge commit on `development`
cleanly removes Room, the bus, and all UI surfaces. The Phase 2.3 surface
(Settings, Calculator screen, theme, DataStore) is untouched by this phase
except for the `CalculatorScreen` icon row and the `CalculatorAction` /
`ExpressionWriter` additions — both purely additive.
